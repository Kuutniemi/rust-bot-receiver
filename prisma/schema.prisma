generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model HeliMinusOne {
  id    Int       @id @default(autoincrement())
  who   String?
  value Int
  when  DateTime? @default(now())
}
